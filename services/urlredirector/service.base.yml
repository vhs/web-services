version: '2.4'

services:
  urlredirector:
    image: vanhack/redirect:latest
    container_name: urlredirector
    restart: always
    volumes:
    - ./services/urlredirector/config.json:/app/config.json
