version: '2.4'

services:
  r:
    container_name: r
    image: vanhack/redirect
    network_mode: bridge
    restart: always
    volumes:
    - ./services/r/config.json:/app/config.json
    environment:
      VIRTUAL_HOST: r.vanhack.ca
      LETSENCRYPT_HOST: r.vanhack.ca
